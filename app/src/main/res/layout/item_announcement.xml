<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_sm"
    app:cardCornerRadius="@dimen/radius_md"
    app:cardElevation="@dimen/elevation_sm"
    app:cardBackgroundColor="@color/background_card"
    android:contentDescription="@string/announcement_item_desc">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/padding_md">

        <TextView
            android:id="@+id/announcementTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_size_normal"
            android:textStyle="bold"
            android:textColor="@color/primary"
            android:contentDescription="@string/announcement_title_desc"
            android:importantForAccessibility="yes"
            tools:text="Important Notice" />

        <TextView
            android:id="@+id/announcementContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:textSize="@dimen/text_size_medium"
            android:textColor="@color/text_primary"
            android:maxLines="3"
            android:ellipsize="end"
            android:contentDescription="@string/announcement_content_desc"
            android:importantForAccessibility="yes"
            tools:text="This is an important announcement about upcoming maintenance work." />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/announcementCategory"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="@dimen/text_size_small"
                android:textColor="@color/secondary"
                android:textStyle="bold"
                android:contentDescription="@string/announcement_category_desc"
                android:importantForAccessibility="yes"
                tools:text="General" />

            <TextView
                android:id="@+id/announcementCreatedAt"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_size_small"
                android:textColor="@color/text_secondary"
                android:contentDescription="@string/announcement_date_desc"
                android:importantForAccessibility="yes"
                tools:text="2024-01-07" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>