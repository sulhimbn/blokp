<?xml version="1.0" encoding="utf-8"?>
<!--
    Backup rules for Android auto backup (API 31+)

    SECURITY: Exclude sensitive data from cloud backup to prevent:
    - API keys or tokens
    - User credentials
    - Financial transaction data
    - Private user information

    Reference: https://developer.android.com/guide/topics/data/autobackup
-->
<full-backup-content>
    <!-- Exclude database files (contains sensitive financial and user data) -->
    <exclude domain="database" path="." />

    <!-- Exclude shared preferences (may contain API tokens or sensitive settings) -->
    <exclude domain="sharedpref" path="." />

    <!-- Exclude cache and temporary files -->
    <exclude domain="root" path="cache" />
    <exclude domain="file" path="no_backup" />

    <!-- Exclude app-specific directories that may contain sensitive data -->
    <exclude domain="file" path="." />
</full-backup-content>