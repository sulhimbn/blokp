<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for Android API 31+

    SECURITY: Exclude sensitive data from cloud backup and device transfer to prevent:
    - API keys or tokens
    - User credentials
    - Financial transaction data
    - Private user information

    Reference: https://developer.android.com/about/versions/12/backup-restore
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude database files (contains sensitive financial and user data) -->
        <exclude domain="database" path="." />

        <!-- Exclude shared preferences (may contain API tokens or sensitive settings) -->
        <exclude domain="sharedpref" path="." />

        <!-- Exclude cache and temporary files -->
        <exclude domain="root" path="cache" />
        <exclude domain="file" path="no_backup" />

        <!-- Exclude app-specific directories that may contain sensitive data -->
        <exclude domain="file" path="." />
    </cloud-backup>

    <device-transfer>
        <!-- Apply same restrictions for device transfer -->
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="root" path="cache" />
        <exclude domain="file" path="no_backup" />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>