# Environment Variables Configuration
# Copy this file to .env and configure values for your environment

# Docker Environment
# Set to "true" or "1" to use mock API endpoints (for Docker/local development)
# Leave unset or set to "false" to use production API endpoints
DOCKER_ENV=

# API Configuration
# Note: Production/Mock API URLs are configured in Constants.Api
# Note: API spreadsheet ID is configured in build.gradle (BuildConfig.API_SPREADSHEET_ID)
# To use different spreadsheet per build variant, override in buildTypes section:
#   buildTypes {
#       debug {
#           buildConfigField "String", "API_SPREADSHEET_ID", "\"DEBUG_SHEET_ID\""
#       }
#       release {
#           buildConfigField "String", "API_SPREADSHEET_ID", "\"PRODUCTION_SHEET_ID\""
#       }
#   }
